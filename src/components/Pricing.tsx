import { ArrowUpRight, Check } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
interface PricingProps { onNavigate: (section: string) => void; data: { heading: { headingP1: string; headingP2: string }; subHeading: string; plans: { name: string; price: string; description: string; badge?: string; highlight?: boolean; features: string[]; cta: string }[]; specialSection: { question: string; answer: string; actionButton: string } }; }
export const Pricing = ({ data }: PricingProps) => { const navigate = useNavigate(); return <section id="pricing" className="section pricing-section"><div className="shell">
  <header className="section-head"><div><p className="eyebrow">Engagements / Starting points</p><h2>{data.heading.headingP1}<em>{data.heading.headingP2}</em></h2></div><p>{data.subHeading}</p></header>
  <div className="price-grid">{data.plans.map((plan, i) => <article key={plan.name} className={plan.highlight ? 'featured-price' : ''}><div className="price-top"><span>0{i + 1}</span>{plan.badge && <small>{plan.badge}</small>}</div><h3>{plan.name}</h3><p>{plan.description}</p><strong>{plan.price}</strong><ul>{plan.features.slice(0, 5).map(feature => <li key={feature}><Check size={15}/>{feature}</li>)}</ul><button onClick={() => navigate('/meeting')}>{plan.cta}<ArrowUpRight size={17}/></button></article>)}</div>
  <div className="custom-cta"><div><p className="eyebrow">Custom scope</p><h3>{data.specialSection.question}</h3></div><p>{data.specialSection.answer}</p><button className="button button-light" onClick={() => navigate('/meeting')}>{data.specialSection.actionButton}<ArrowUpRight size={17}/></button></div>
  </div></section>; };
